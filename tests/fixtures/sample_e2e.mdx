# E2E Test: Real MDX Document

export const meta = { title: 'E2E', tags: ['test', 'mdx'] }

This is **MDX** with JSX and code blocks.

<Button onClick={() => alert('ok')}>Click</Button>

<Alert type="info">
  Works with nested markdown like **bold** and `inline code`.
</Alert>

```js
console.log('code block');
```
